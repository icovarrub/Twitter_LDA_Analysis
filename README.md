# Twitter_LDA_Analysis

# Use R to do topic modeling analysis on Twitter data
# After cleaning and formatting the raw text, topics were generated using an unsupervised topic model algorithm called Latent Dirichlet Allocation (LDA) (Blei, 2003).
